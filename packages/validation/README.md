# packages/validation

Zod Schemas مشتركة لكل نموذج بيانات بالنظام (منتج، فاتورة، عميل، مورد...) — نفس الـ Schema
تُستخدم بالفورم (client-side) وبالـ Server Action/RPC call (server-side، وهو التحقق الملزم).
راجع CLAUDE.md §6.4.
