// هذا الملف يُولَّد تلقائيًا من قاعدة البيانات الفعلية عبر:
//   supabase gen types typescript --project-id <project-id> > packages/database/types.ts
// لا تُعدَّل يدويًا — أي تعديل هنا سيُفقد عند إعادة التوليد.
// لا يوجد حاليًا مشروع Supabase Cloud مربوط بهذا المستودع، لذلك هذا الملف Placeholder
// حتى يتم تطبيق migrations/ على مشروع حقيقي وإعادة التوليد.

export type Database = Record<string, never>;
